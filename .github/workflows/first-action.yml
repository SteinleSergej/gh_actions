name: Test Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get The Code
        uses: actions/checkout
      - name: Install Dependencies
        run: npm ci          # like install but use logged versions
      - name: Test Code
        run: npm test